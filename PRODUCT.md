# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 主要用户是需要亲自做内容获客的老板、个体户和小微企业主；他们通常懂自己的生意，但不熟悉选题、脚本、分镜和剪辑术语。
- 用户多在 Windows/macOS 桌面端整理资料和素材，在手机上完成实拍；首个使用场景以中文竖屏短视频为主。

## Product Purpose

把老板本人、经营业务和可用实拍素材沉淀为可复用的创作资料，再把一个视频意图依次转成个性化脚本、逐镜素材方案和可审阅的成片提案。成功意味着用户不需要先学会编导和剪辑，也能知道“说什么、每一段放什么画面、还缺什么、下一步按哪个按钮”。

## Positioning

产品不是从一张空白脚本开始，而是从经用户确认的“老板资料卡 + 生意资料卡 + 本人素材库”开始；AI 的每一段文字和每一个镜头都必须能回指使用了哪些资料、匹配了哪条素材，以及哪些内容仍是待确认或生成提案。

## Operating Context

- 首次建档分为最小必填资料、按脚本模板动态追问、真人素材逐条授权三层；不要求一次填完所有字段。
- 用户可以从固定脚本模板开始，也可以输入想做的视频；AI 先生成可编辑结构，再用已确认资料填充内容。
- 脚本支持保留当前版本并“一键换一版”，不能静默覆盖用户修改。
- 系统先为分镜匹配用户上传的实拍素材；未匹配镜头可批量发起生成提案，涉及费用和肖像/声音处理时必须确认。
- 所有镜头都有明确素材状态，只有在脚本版本和镜头素材冻结后，才允许创建拼合/剪辑提案。

## Capabilities and Constraints

- 本地优先：资料、脚本、分镜、素材索引和项目状态以本地工作区为事实源。
- AI 先提案、人可审阅：脚本、素材匹配、生成素材和拼合结果都必须可预览、替换、重试和回退。
- 用户资料只采集生成脚本所需的最小范围；年龄默认采集年龄段，对外称呼不等同实名。
- 不得根据资料擅自补写个人经历、资历、经营数据、效果承诺或敏感身份。
- 当前仓库是可运行的早期产品 scaffold；本轮交付实现可验证的前端垂直切片和领域规则，不把真实付费生成、自动渲染或发布描述成已经上线。

## Brand Commitments

- 产品名：原点（Creator Copilot Local）。
- 语言：直接、克制、可执行，不用编导黑话要求老板理解系统。
- 既有视觉基调是适合桌面长时间操作的暖中性色和低饱和绿色，本轮扩展保持这一识别。

## Evidence on Hand

- 现有 PRD、架构和失败路径文档位于 `docs/`。
- 现有 React/Electron scaffold 已具备脚本提案、分镜、拍摄包、素材导入、AI 剪辑和导出相关边界；是否可用以真实测试和实现为准。
- 当前没有真实老板用户访谈、完成率基线、生成质量基线或商业效果数据；界面中的示例内容必须标为演示。

## Product Principles

1. 先认识这个人和这门生意，再写脚本。
2. 每次只收集当前视频真正需要的资料。
3. 先用真实素材，缺口再生成；生成永远是可审阅提案。
4. 每一步都显示来源、缺口、状态和下一动作。
5. 用户改过的内容不能被“一键生成”静默覆盖。

## Accessibility & Inclusion

- 不用真实年龄、性别、口音或经营规模推断能力、可信度和内容风格。
- 关键状态不能只靠颜色表达；表单、步骤、上传和生成操作必须支持键盘和清晰焦点。
- 动效尊重 `prefers-reduced-motion`，核心流程在桌面窄窗口下仍可完成。
